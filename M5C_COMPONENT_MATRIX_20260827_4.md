# M5c — матрица компонентов на 2026-08-27 (редакция 4)

Состояние **ядра 4.9.188-m5c+** под LineageOS 14.1. Редакция 4 снята после
того, как в аппарат вставили SIM-карту: это разом закрыло вопрос по модему и
вскрыло критический дефект в Wi-Fi, из-за которого аппарат перезагружался.

Рабочий образ для тестера — `artifacts/boot_m5c_49_20260827_wlanfix.img`
(md5 `88ed99c6b7f57b04c50b33f3ba0985f3`). Он же прошит на устройстве.
Предыдущий, `boot_m5c_49_20260827_modem.img` (md5
`30ec1334a99f6adb2d461e1a5beba18f`), **непригоден для повседневной работы**:
на нём включение Wi-Fi роняет ядро.

Аппаратная привязка — `M5C_CHIP_MAP.md` (снята со стока), хронология и
доказательства — `BRINGUP_STATE.md`. Предыдущая редакция — `_3.md`.

Статусы:

- **РАБОТАЕТ** — проверено в рантайме на этом ядре
- **ЧАСТИЧНО** — поднимается, но не выполняет свою задачу целиком
- **СЛОМАНО** — код есть, на железе не работает, причина известна
- **ОТКРЫТО** — причина не установлена
- **N/A** — железа на экземпляре нет

## 1. Камера

| узел | чип | адрес | статус | детали |
|---|---|---|---|---|
| `camera_main` | **Samsung S5K4H8** (8 Мп, задняя) | `i2c-0` 0x10 (write id 0x20) | **РАБОТАЕТ** | `Number of camera devices: 1`, Facing BACK, `sensor id: 0x4088`, открывается и снимает |
| `camera_main_af` | **DW9714** (привод автофокуса) | `i2c-0` 0x18 | РАБОТАЕТ | `MAINAF [AFRegulatorCtrl] regulator_get` в буте |
| `camera_sub` | **Samsung S5K5E8YX** (5 Мп, фронтальная) | `i2c-0` 0x3c (write id 0x78) | **СЛОМАНО (похоже, железо)** | питание, сброс, power-down, такт и шина приведены к стоку 1:1, сенсор всё равно отвечает `I2C_ACKERR` на всех адресах |
| `strobe_main` | **TI LM3642** (вспышка) | `i2c-1` 0x63 | РАБОТАЕТ | `/dev/kd_camera_flashlight`, torch-статус меняется |
| ISP / seninf | MT6737M | — | РАБОТАЕТ | `/dev/camera-isp`, `camera-sysram`, `camera-pipemgr` |
| OTP-каналы | 4 варианта S5K4H8 + 4 S5K5E8 | — | РАБОТАЕТ | все восемь `/dev/S5K*_OTP` зарегистрированы |

## 2. Связь

| подсистема | чип | статус | детали |
|---|---|---|---|
| Wi-Fi | **MT6735 CONSYS** (SOC, `consys@18070000`) | **РАБОТАЕТ** | `wlan.driver.status = ok`, `wlan0` UP, MAC `d8:6c:02:ab:6f:3e`, скан отдаёт точки |
| Wi-Fi: стабильность ядра | — | **ПОЧИНЕНО 2026-08-27** | до этой сборки включение Wi-Fi роняло ядро: `wext_set_country` читал user-указатель напрямую, без `copy_from_user`, что на 4.9 с PAN даёт `Accessing user space memory outside uaccess.h routines` и `exception reboot`. Заодно закрыты такие же места в `gl_wext_priv.c` |
| Bluetooth | тот же CONSYS | **РАБОТАЕТ** | `state: ON`, `BT_open: STP is ready!`; адрес `22:22:AF:F6:76:58` дефолтный, не из NVRAM |
| GPS (ядро) | тот же CONSYS | РАБОТАЕТ | `/dev/stpgps`, `mnld` живёт |
| GPS (демон) | — | **РАБОТАЕТ** | `mtk_agpsd` из сборки m681 (ICU 56 — версия этого ROM), `init.svc.agpsd = running`, `ver=4.151.0`, включены GPS/GLONASS/Beidou/Galileo |
| GPS (фикс позиции) | — | **СЛОМАНО** | `mnld` в цикле: `main: process data error: 2`, обмен с agpsd даёт `unknown type=252` |
| FM | CONSYS | ОТКРЫТО | не проверялось |

## 3. Модем и сотовая связь

| элемент | статус | детали |
|---|---|---|
| Загрузка образа MD | **РАБОТАЕТ** | `Request firmware: dsp_1_lwg_n.bin`, VLTE подаётся, `md_power_on ret=0`, `cldma_start` |
| Хендшейк с MD | **РАБОТАЕТ** | HS1/HS2 проходят, `md1:4/0` |
| Узлы `/dev/ccci_*` | РАБОТАЕТ | добавлены семь, которые открывает стоковый демон |
| Файловый сервер | **РАБОТАЕТ** | `ccci_fsd` из дерева m681 (`md_fsd Ver:v2.1, CCCI Ver:6`) |
| Мультиплексор | **РАБОТАЕТ** | `gsm0710muxd` держит `/dev/ttyC0`, создаёт `/dev/radio/*`, `Frames received/dropped: 544/0` |
| Автозапуск цепочки | **РАБОТАЕТ (новое)** | `mtk.md1.status=init_done` → mux → RIL, всё из init, до телефонного процесса |
| RIL ↔ модем | **РАБОТАЕТ** | `mtkrild` гоняет AT непрерывно, `vendor RIL version: 10` |
| **Чтение SIM модемом** | **РАБОТАЕТ** | `+CPIN: READY`, `+CRSM: 144,0,...` отдаёт EF_DIR с USIM AID `A0000000871002`, ICCID виден в `persist.radio.ia` |
| **Регистрация в сети** | **РАБОТАЕТ** | `gsm.operator.alpha = beeline`, `gsm.operator.numeric = 25099`, сигнал около -54 dBm |
| **Статус SIM во фреймворке** | **СЛОМАНО** | `gsm.sim.state = NOT_READY`. `GET_SIM_STATUS` уходит в `mtkrild`, обработчик выполняет AT-команды, но ответа наверх нет ни разу за загрузку; радио остаётся `RADIO_OFF`, `AT+CFUN=1` не отправляется. Разбор — `M5C_SIM_READY_LANE.md` |
| Звонки, SMS, данные | не проверялись | заблокированы статусом SIM |

## 4. Дисплей и ввод

| подсистема | чип | статус | детали |
|---|---|---|---|
| Панель | **ILI9881c** (720×1280, 58.5 Гц) | РАБОТАЕТ | картинка есть, подсветка управляется |
| Bias | **LP3101** | РАБОТАЕТ | `i2c-1` 0x3e |
| Композиция | собственный HWC (`forge_hwc.c`), HW-оверлеи | РАБОТАЕТ | GPU вне композиции |
| **Разрывы кадра** | — | **ОТКРЫТО** | ядро исключено доказательно (конфиг-батч латчится в бланкинге, trigger loop жив); HWC инструментирован — fence-и честные. Источник выше HWC, в SurfaceFlinger/hwui |
| Тач | **Goodix GT917D** | РАБОТАЕТ | `i2c-1` 0x5d, реальные касания |
| Клавиши | `mtk-kpd` | РАБОТАЕТ | input1 |
| Гарнитура | `ACCDET` | РАБОТАЕТ | input0 |
| Вибратор | — | РАБОТАЕТ | `/sys/class/leds/vibrator` |
| Подсветка | — | РАБОТАЕТ | `/sys/class/leds/lcd-backlight` |

## 5. Сенсоры

| сенсор | чип | адрес | статус |
|---|---|---|---|
| Акселерометр | **mCube MC3XXX** | `i2c-2` 0x18 | РАБОТАЕТ |
| Магнитометр | **AKM AK09912** | `i2c-2` 0x0c | РАБОТАЕТ |
| Свет + приближение | **Sensortek STK3x1x** | `i2c-2` 0x48 | РАБОТАЕТ |
| Ориентация / гравитация / линейное ускорение / rotation vector | виртуальные | — | РАБОТАЕТ |
| Гироскоп | — | `i2c-2` 0x68 (посадочное место) | **N/A** — физически нет |
| Дактилоскоп | — | — | **N/A** |
| NFC | — | `i2c-2` 0x28 | **N/A** — не биндится и в стоке |

## 6. Питание, звук, хранение

| подсистема | чип | статус |
|---|---|---|
| Зарядник | **Fairchild FAN5405** (`i2c-1` 0x6a) | РАБОТАЕТ |
| PMIC | **MT6328** | РАБОТАЕТ |
| Топливомер | MTK FG | РАБОТАЕТ |
| RTC | — | РАБОТАЕТ |
| WDT | — | РАБОТАЕТ |
| Звук | MT6328 codec | РАБОТАЕТ (`/dev/snd`: controlC0, pcmC0D0..D9) |
| eMMC | msdc0 (`mediatek,mt6735m-mmc`) | РАБОТАЕТ — HS400, autok pass |
| USB | musb, configfs | РАБОТАЕТ (adb) |
| CPU | 4 × Cortex-A53, hotplug | РАБОТАЕТ |

## 7. Открытые фронты, по приоритету

1. **Статус SIM во фреймворке.** Всё железо и весь модемный стек работают,
   упирается в `GET_SIM_STATUS` внутри `mtkrild`. Это последний шаг до
   звонков и мобильных данных.
2. **Разрывы изображения.** Ядро и HWC исключены; смотреть
   SurfaceFlinger/hwui.
3. **GPS — фикс позиции.** `mnld` не находит какой-то файл и расходится с
   `agpsd` по типу сообщения.
4. **BT-адрес** из NVRAM вместо дефолтного (хелпер написан, нужна сборка ROM).
5. **Фронтальная камера — вероятно, железо.** Программная сторона исчерпана.

## 8. Что изменилось против редакции 3

- Модем прошёл весь путь: mux работает, RIL общается, SIM читается,
  **аппарат зарегистрирован в сети Beeline**. В редакции 3 здесь стояло
  «`gsm0710muxd` не открывает свой порт».
- Найден и исправлен дефект, из-за которого Wi-Fi ронял ядро. Он же
  объясняет «самопроизвольные» разрывы adb и перезагрузки в прошлых сессиях.
- Цепочка модема стартует автоматически из init, а не руками.

## 9. Инфраструктурные заметки

- USB-узел телефона на девбоксе появляется как `root:root` (в контейнере нет
  udev-правил), и adb его не видит. Хелпер `usbfix.sh` в скретчпаде находит
  узел по серийнику и открывает; номер меняется после каждой перезагрузки.
- Гейт упаковки: `Image.gz` + `dtb_stock.dtb`, проверять
  `strings boot.img | grep -c mt6735m-mmc` == 2 и
  `grep -c 'mediatek,msdc'` == 0. Если правится ramdisk — пересобирать его
  (`pack_rd.sh`), обычный `pack.sh` обновляет только ядро.
- Пересборка того же состояния дерева однажды дала незагружающийся образ при
  пустом `git diff` — проверять загрузку каждого образа отдельно.
- Никогда не писать `/*` или `*/` внутри тела комментария в ядре: дважды
  ломало сборку.
