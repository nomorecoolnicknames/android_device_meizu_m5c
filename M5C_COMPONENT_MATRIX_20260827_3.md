# M5c — матрица компонентов на 2026-08-27 (редакция 3)

Состояние **ядра 4.9.188-m5c+** под LineageOS 14.1. Редакция 2 отражает
работу по модему и фронтальной камере, проделанную после первой.
Базовый рабочий образ для тестера —
`artifacts/boot_m5c_49_20260827.img` (md5 `4b4b9a35382273d6e3bf9e03222f0e2f`).
Предыдущая редакция — `M5C_COMPONENT_MATRIX.md` (2026-08-17), она описывала
состояние до того, как связь, камера и модем поехали; сравнивать с ней
имеет смысл только исторически.

Аппаратная привязка — `M5C_CHIP_MAP.md` (снята со стока), хронология и
доказательства — `BRINGUP_STATE.md`.

Статусы:

- **РАБОТАЕТ** — проверено в рантайме на этом ядре
- **ЧАСТИЧНО** — поднимается, но не выполняет свою задачу целиком
- **СЛОМАНО** — код есть, на железе не работает, причина известна
- **ОТКРЫТО** — причина не установлена
- **N/A** — железа на экземпляре нет

## 1. Камера

| узел | чип | адрес | статус | детали |
|---|---|---|---|---|
| `camera_main` | **Samsung S5K4H8** (8 Мп, задняя) | `i2c-0` 0x10 (write id 0x20) | **РАБОТАЕТ** | `Number of camera devices: 1`, Facing BACK, `sensor id: 0x4088`, открывается и снимает |
| `camera_main_af` | **DW9714** (привод автофокуса) | `i2c-0` 0x18 | РАБОТАЕТ | `MAINAF [AFRegulatorCtrl] regulator_get` в буте |
| `camera_sub` | **Samsung S5K5E8YX** (5 Мп, фронтальная) | `i2c-0` 0x3c (write id 0x78) | **СЛОМАНО (похоже, железо)** | питание, сброс, power-down, такт и шина приведены к стоку 1:1, сенсор всё равно отвечает `I2C_ACKERR` на всех адресах; см. §7 |
| `strobe_main` | **TI LM3642** (вспышка) | `i2c-1` 0x63 | РАБОТАЕТ | `/dev/kd_camera_flashlight`, torch-статус меняется |
| ISP / seninf | MT6737M | — | РАБОТАЕТ | `/dev/camera-isp`, `camera-sysram`, `camera-pipemgr` |
| OTP-каналы | 4 варианта S5K4H8 + 4 S5K5E8 | — | РАБОТАЕТ | все восемь `/dev/S5K*_OTP` зарегистрированы |

Ключ к работающей камере (2026-08-25): ABI структуры `ACDK_SENSOR_INFO_STRUCT`
в дереве — 212 байт, блобы ROM ждут 144 (`GETINFO`) и 192 (`GETINFO2`);
`copy_to_user` переполнял буфер HAL на 68 байт и затирал его массив
указателей, отчего `mediaserver` падал в цикле ещё до конца перебора
сенсоров. Лечится маршалингом в legacy-раскладку.

## 2. Связь

| подсистема | чип | статус | детали |
|---|---|---|---|
| Wi-Fi | **MT6735 CONSYS** (SOC, `consys@18070000`) | **РАБОТАЕТ** | `wlan0`, MAC `d8:6c:02:ab:6f:3e`, скан отдаёт 20 точек |
| Bluetooth | тот же CONSYS | **РАБОТАЕТ** | `state: ON`, `BT_open: STP is ready!`; адрес `22:22:AF:F6:76:58` выглядит дефолтным, не из NVRAM |
| GPS (ядро) | тот же CONSYS | РАБОТАЕТ | `/dev/stpgps`, `mnld` живёт |
| GPS (демон) | — | **РАБОТАЕТ** | линковочный блокер снят: m681-сборка `mtk_agpsd` собрана под ICU 56 — ту, что в этом ROM. `init.svc.agpsd = running`, `ver=4.151.0`, включены GPS/GLONASS/Beidou/Galileo |
| GPS (фикс позиции) | — | **СЛОМАНО** | `mnld` в цикле: `main: process data error: 2 (No such file or directory)`, обмен с agpsd даёт `unknown type=252`; фикса нет |
| FM | CONSYS | ОТКРЫТО | не проверялось |

Ключи к работающей связи (2026-08-25): `f_op->read` == NULL на ext4 в 4.9
(конфиг `WMT_SOC.cfg` «не читался»); падение error-path на
незарегистрированном wakeup source превращало любую ошибку в бутлуп;
BTIF DMA искал регистры в узле не той формы, чем в стоковом DTB.

## 3. Модем

| элемент | статус | детали |
|---|---|---|
| Загрузка образа MD | **РАБОТАЕТ** | `Request firmware: dsp_1_lwg_n.bin ... to 0xbaa20000`, VLTE подаётся, `md_power_on ret=0`, `cldma_start` |
| Хендшейк с MD | **РАБОТАЕТ** | `event 1 ... from port_proxy_md_hs1_msg_notify`, `BootChannel 0` |
| Узлы `/dev/ccci_*` | РАБОТАЕТ | добавлены семь, которые открывает стоковый демон |
| Триггер старта | РАБОТАЕТ | `/sys/class/BOOT/BOOT/boot/md` опубликован |
| Сетевые интерфейсы | поднимаются | `ccmni0..17` присутствуют |
| Стабильность MD | **РАБОТАЕТ** | exception устранён: `early exception` больше не возникает, модем держится в `md1:4/0`, проходит HS2 и шлёт RPC |
| Файловый сервер | **РАБОТАЕТ** | демон из дерева m681 (`md_fsd Ver:v2.1, CCCI Ver:6`) совместим с нашим ccci; стартует автоматически и обслуживает запросы MD |
| RIL | ЧАСТИЧНО | `mtkrild` запускается, видит `SIM_COUNT: 2`, поднимает vsim-сокет |
| **Регистрация в сети** | **СЛОМАНО** | `gsm0710muxd` не открывает свой порт (в его справке путь `[(null)]`), без него нет `/dev/radio/pttycmd*`, которых ждёт RIL: `could not connect to /dev/radio/pttynoti`. `gsm.sim.state = NOT_READY` |

Ловушки, стоившие итераций:
- при `CONFIG_MACH_MT6735M=y` собирается
  `drivers/misc/mediatek/eccci1/`, а **не** `eccci/`;
- стоковый `ccci_fsd` из vendor-дерева m5c написан под legacy
  `dual_ccci` ABI (`_IO('K',1)`), которого наше ядро не реализует: он
  получал `ENOTTY`, падал с SIGSEGV, модем оставался без NVRAM и падал
  сам. Замена на сборку из m681 сняла это;
- `ccci_mdinit` из `class core` стартует до готовности NVRAM и выходит с
  255; ему нужен триггер `on property:service.nvram_init=Ready` и запуск
  от root.

## 4. Дисплей и ввод

| подсистема | чип | статус | детали |
|---|---|---|---|
| Панель | **ILI9881c** (`lcm=1-ili9881c_dsi_vdo_dj_hd720`, 720×1280, 58.5 Гц) | РАБОТАЕТ | картинка есть, подсветка управляется |
| Bias | **LP3101** | РАБОТАЕТ | `i2c-1` 0x3e |
| Композиция | собственный HWC (`forge_hwc.c`), HW-оверлеи | РАБОТАЕТ | GPU вне композиции |
| **Разрывы кадра** | — | **ОТКРЫТО** | ядро исключено доказательно: конфиг-батч латчится в вертикальном бланкинге, trigger loop жив, время ожидания CMDQ размазано 18 мкс…16 мс. Причина в юзерспейсе (fence/контент), нужна пересборка `.so` |
| Тач | **Goodix GT917D** (`gt9xx`, hotknot-вариант) | РАБОТАЕТ | `i2c-1` 0x5d, `mtk-tpd` + `mtk-tpd-kpd`, реальные касания |
| Клавиши | `mtk-kpd` | РАБОТАЕТ | input1 |
| Гарнитура | `ACCDET` | РАБОТАЕТ | input0 |
| Вибратор | — | РАБОТАЕТ | `/sys/class/leds/vibrator` |
| Подсветка | — | РАБОТАЕТ | `/sys/class/leds/lcd-backlight` |

Отдельно: тач делит линию **VGP1** с цифровым ядром фронтальной камеры.
Драйвер тача больше не пиннит её на 2.8 В — запрашивает диапазон
1.2–2.8 В; на 1.2 В тач работает (проверено касаниями).

## 5. Сенсоры

| сенсор | чип | адрес | статус |
|---|---|---|---|
| Акселерометр | **mCube MC3XXX** | `i2c-2` 0x18 | РАБОТАЕТ (`m_acc_input`) |
| Магнитометр | **AKM AK09912** | `i2c-2` 0x0c | РАБОТАЕТ (`m_mag_input`) |
| Свет + приближение | **Sensortek STK3x1x** | `i2c-2` 0x48 | РАБОТАЕТ (LIGHT, PROXIMITY в `sensorservice`) |
| Ориентация / гравитация / линейное ускорение / rotation vector | виртуальные (AKM + AOSP) | — | РАБОТАЕТ |
| Гироскоп | — | `i2c-2` 0x68 (посадочное место) | **N/A** — физически нет, отдаётся программная эмуляция AKM/AOSP |
| Дактилоскоп | — | — | **N/A** — узлов нет, посадочного места на этом экземпляре не найдено |
| NFC | — | `i2c-2` 0x28 | **N/A** — не биндится и в стоке |

## 6. Питание, звук, хранение

| подсистема | чип | статус |
|---|---|---|
| Зарядник | **Fairchild FAN5405** (`i2c-1` 0x6a) | РАБОТАЕТ |
| PMIC | **MT6328** | РАБОТАЕТ |
| Топливомер | MTK FG | РАБОТАЕТ (уровень, температура) |
| RTC | — | РАБОТАЕТ |
| WDT | — | РАБОТАЕТ (в bring-up-образах — deadman) |
| Звук | MT6328 codec | РАБОТАЕТ (`/dev/snd`: controlC0, pcmC0D0..D9) |
| eMMC | msdc0 (`mediatek,mt6735m-mmc`) | РАБОТАЕТ — HS400, autok pass |
| USB | musb, configfs | РАБОТАЕТ (adb) |
| CPU | 4 × Cortex-A53, hotplug | РАБОТАЕТ (0-3 под нагрузкой) |

## 7. Открытые фронты, по приоритету

1. **Модем — регистрация в сети.** Всё до RIL работает; блокер —
   `gsm0710muxd`. Следующий шаг: сравнить с рабочим MTK-устройством того
   же поколения или собрать mux из исходников, а не подбирать аргументы.
2. **Фронтальная камера — вероятно, железо.** Программная сторона
   исчерпана: сверены со стоком питание (VCAMA 2.8 / VGP1 1.2 /
   VCAMIO 1.8), сброс (`cam1_rst0→cam1_rst1`, GPIO 11, полярность из
   стокового DTB), power-down (`cam1_pnd0`), такт (`ISP_MCLK1_EN`),
   адрес и привязка клиента. Контроллер отвечает `I2C_ACKERR` — на шине
   никто не отзывается. Три версии про скорость шины проверены прошивкой
   и отвергнуты. Проверять заменой модуля/шлейфа.
3. **Разрывы изображения.** Ядро чисто; проверять fence/контент в
   `forge_hwc.c`, требуется пересборка одного `.so`.
4. **GPS — фикс позиции.** Демон запускается; `mnld` не находит какой-то файл и расходится с ним по типу сообщения. Проверяется пара `mnld` из того же m681-дерева.
5. **BT-адрес** из NVRAM вместо дефолтного.

## 8. Инфраструктурные заметки

- USB-узел телефона на девбоксе появляется как `root:root` (в контейнере
  нет udev-правил), и adb его не видит. Хелпер `usbfix.sh` в скретчпаде
  находит узел по серийнику и открывает; номер меняется после каждой
  перезагрузки.
- Гейт упаковки: `Image.gz` + `dtb_stock.dtb`, проверять
  `strings boot.img | grep -c mt6735m-mmc` == 2 и
  `grep -c 'mediatek,msdc'` == 0.
- Пересборка того же состояния дерева однажды дала незагружающийся образ
  при пустом `git diff` — проверять загрузку каждого образа отдельно.
