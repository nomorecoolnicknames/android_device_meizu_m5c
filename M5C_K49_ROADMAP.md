# M5C_K49_ROADMAP — от «adb на 4.9» к полному ядру, новым ОС и семейству MT6753

Дата: 2026-08-20. Точка отсчёта: p41 (`086f17767`, ветка `m5c-arm64`) — LOS
14.1 грузится на ядре 4.9 до рабочего adb (`M5C_K49_DEEP_DIVE.md` §11.13,
`BRINGUP_STATE.md` P37–P41). Дисплей/GPU/сенсоры/連 — не смержены; экран
на LK-логотипе. Evidence-канал: eMMC-зеркало в expdb (слоты+rc49+ранний
снапшот) — рабочий и OS-независимый.

Состояние офлайн-веток (`/srv/forge/android/m5c/k49-worktrees/`, все
собираются, на железе не прогнаны): gpu49 (mali r7p0, Phase E build-green),
sensors49 (mc3410/akm09912/stk3x1x), conn49 (wmt/wlan-gen2/gps/fm, Phase
H-3), aud49 (mt_soc, Phase C), av49 (eccci1 модем + vdec/venc/jpeg), cam49
(s5k4h8/s5k5e8+DW9714+smi/mmdvfs). **Ветки disp49 НЕТ — дисплей не начат.**

---

## Фронт A — полноценное ядро m5c (reference-ОС остаётся LOS 14.1)

### A0. Стабилизация базы (1–2 прошивки, до всего остального)
- **A0.1** Диагноз возврата в recovery ~215с по живому adb: logcat/
  tombstones. HYPOTHESIS: crash-loop critical-сервиса (surfaceflinger без
  display/GPU HAL) → init через 4 краша уходит в recovery. Критерий FACT:
  строка init «critical service … rebooting into recovery» в logcat.
- **A0.2** Валидация p32 mt_gpt: тот же p41-бинарь БЕЗ `idle=poll`.
  Критерий: пульсы 95/96 живут >5 мин, adb стабилен. Успех = `idle=poll`
  снимается насовсем (питание/тепло).
- **A0.3** WDT DDR-reserve: p37-факт — после HW-WDT-ресета DRAM=0xFF.
  Включить DDR-reserve бит в `mtk_wdt_mode_config` (как в
  `wd_sw_reset(KEEP_DDR_RESERVE)`) → тёплые ресеты снова сохраняют DRAM,
  возвращается второй канал улик.
- **A0.4** Отладочная гигиена: приглушить `usb_state<>`-монитор (3с) и
  MUSB connection-спам (замусоривают rc49-ринг); rc49/pstore вернуть на
  стоковые окна (0x43f00000/0x43f10000) ТОЛЬКО когда adb стабилен —
  оживёт штатный `/proc/last_kmsg`. Дедмэн остаётся (здоровые буты он
  больше не трогает — forge_userspace_alive).

### A1. ДИСПЛЕЙ — критический путь (новая ветка forge/disp49)
- Порт dispsys mt6735 (disp_drv/ddp/primary_display) из Q0 + lcm-драйвер.
- **Наш экземпляр 710HVBR923RYK = ILI9881C «dj» + bias lp3101** (не jd9365!
  реверс уже сделан в 3.18-порте — таблицы/DSI брать оттуда/из стокового
  vmlinux байт-в-байт, метод «8 из 8»).
- LK поднимает панель до ядра (`is_inited`) → цель №1: подхватить живой
  DSI/фреймбуфер БЕЗ реинициализации (как сток), resume-путь — потом.
  Ловушка из 3.18-опыта: init-таблица исполняется только на resume —
  ошибка в ней проявится после первого гашения, не на буте.
- Verification: запись в /dev/graphics/fb0 видна; затем surfaceflinger
  перестаёт крашиться → A0.1-петля исчезает сама.

### A2. Тач GT917 (порт gt9xx-наработок 3.18) + mBack/клавиши.

### A3. Мерж офлайн-веток — по одной, каждая со своей прошивкой и чек-листом
Порядок: **gpu49** (сразу после A1) → **sensors49** → **conn49**
(wifi→bt→gps→fm) → **aud49** → **av49** (модем — самый рискованный,
последним из инфраструктурных) → **cam49**. Правило: новые probe-пути
получают маркеры; регресс любой ветки не блокирует остальные (ветки
независимы от HEAD).

### A4. Питание: cpuidle без poll (после A0.2), DVFS/thermal, полный цикл
зарядки, suspend/resume (+ ESD/resume дисплея из A1).

### A5. Релизная гигиена: сверить фактическую LTS-базу (uname говорит
`4.9.188-m5c+`, деп-дайв заявлял 4.9.337 — расхождение проверить) и домержить
LTS-хвост до 4.9.337 (EOL); DEVMEM/permissive/дедмэн — за diag-конфигами.

---

## Фронт B — новые версии ОС на этом ядре

Стратегия: 4.9 штатно несёт Android 8.1–11 (LOS 15.1/16.0/17.1/18.1).
Лестница по одной ступени; reference 14.1 всегда остаётся рабочей.

- **B0. Предпосылки — уже выполнены:** configfs-гаджет на 4.9 доказан
  (p30: хост видел 18d1:d001 через configfs-эмуляцию android0);
  p38-гарды (`#ifndef CONFIG_USB_G_ANDROID`) автоматически возвращают
  configfs-путь при выключении G_ANDROID → **переход USB на 15.1+ = один
  конфиг-свитч**, f_adb/legacy остаются только для 14.1-со-сток-рамдиском.
  JDK8-инфра для Oreo на build station уже есть.
- **B1. LOS 15.1 (8.1):** СВОЙ рамдиск (ограничение «рамдиск общий со
  стоком» умирает — сток-бут остаётся отдельным boot-образом-бэкапом);
  binder-девайсы binder/hwbinder/vndbinder (4.9 ok); sdcardfs; sepolicy
  8.1; HALы passthrough (non-treble legacy device path); USB через
  configfs (`sys.usb.configfs=1`).
- **B2. LOS 16.0 (9):** обновление device-дерева, sepolicy; по-прежнему
  non-treble допустим.
- **B3. LOS 17.1 (10):** прямой опыт NX569J «Android 10 на ядре 3.10» —
  блокеры были ROM-side (fstab forceencrypt, /firmware,/dsp,/persist) —
  тот чек-лист применить превентивно. Быстрая валидация: phh-GSI поверх
  нашего vendor до полной сборки.
- **B4 (опция). LOS 18.1 (11)** — 4.9 допустим для upgrade-девайсов.
- Правило фронта: каждый шаг верифицируется expdb-каналом (OS-независим);
  наверх не идём, пока текущая ступень не бутается стабильно.

---

## Фронт C — адаптация 4.9 на m2note / m5s (оба MT6753)

- **C0. FACT:** BSP k37mv1 уже несёт `CONFIG_MACH_MT6753`
  (base/power/Makefile: spm_v1, cpuidle_v1, платформенная обвязка) — m5c
  сидит на `MACH_MT6735M`, это варианты ОДНОЙ платформы mt6735. Дерево
  одно; отдельный форк не нужен.
- **Стратегия:** ветка `forge/mt6753-49` от `m5c-arm64`; device-дельта =
  сток-DTB каждого аппарата (байт-в-байт appended, как на m5c) + defconfig
  + SIP-таблица под ИХ сток-ATF.
- **C1. Чек-лист переноса** (кодифицированные грабли m5c — проходить ДО
  первой прошивки):
  1) сток-DTB байт-в-байт, LK правит memory node сам;
  2) **SIP-нумерация их сток-ATF** (2015–16 — возможно третья таблица;
     методика P19: живой замер CNTPCT/CNTP_CTL/GICD из их TWRP на стоке);
  3) msdc: host id по node name + **НЕ переименовывать bootdevice** (p40);
  4) class clash configfs/android.c — уже закрыт p38 в общем дереве;
  5) **f_adb уже в ядре** (их LOS 14.1 рамдиски — тот же legacy-путь);
  6) DRAM-окна маркеров/rc: карта памяти другая — валидировать
     write-reboot-read из ИХ recovery (как rev3 на m5c), затем
     memblock_reserve под их адреса;
  7) evidence-канал: expdb в их GPT есть — зеркало переносится, MKDEV
     сверить по их by-name (номер партиции может отличаться!);
  8) mt_gpt/arch-timer broadcast — тот же класс проблем (p32-фикс общий);
  9) 8×A53: hps/DVFS/EEM таблицы mt6753 (spm_v1/cpuidle_v1 ветки BSP);
  10) PMIC: у MT6753 пара — **MT6325** (не MT6328, как на m5c) —
     pwrap/pmic порт проверить отдельно, это главный HW-делта-риск.
- **C2. Порядок:** сначала **m5s** (свежий план и дерево
  `/srv/forge/android/m5s`, железо идентифицировано: панель ili9881 yassy,
  тач ft5x0x@0x38; доступ проще), затем **m2note** (golden reference LOS
  14.1 есть, но дисциплина прошивки строже: adb-туннель 15038, только p9,
  gunzip+md5 перед прошивкой, corrupt-rescue история).
- **C3. Этапы на каждом устройстве:** E0 «p41-морда» (preloader→LK→kernel→
  adb, с зеркалом улик) → E1 display/touch → E2 остальное в A3-порядке.

---

## Ближайшие 5 действий
1. A0.2 — прошить p41-бинарь без `idle=poll` (валидация p32 mt_gpt).
2. A0.1 — logcat/tombstones по живому adb: подтвердить SF-петлю ~215с.
3. A1 — старт `forge/disp49`: dispsys + ILI9881C dj из 3.18-реверса.
4. A0.3 — DDR-reserve бит WDT (вернуть DRAM-канал улик).
5. C1.2 — параллельно: P19-замер SIP/таймера на стоке m5s из TWRP.
